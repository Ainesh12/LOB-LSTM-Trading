[project]
name = "lob-lstm-trading"
version = "0.1.0"
description = "Deep learning limit order book trading backtest"
requires-python = ">=3.9"

[tool.black]
line-length = 88
target-version = ["py39"]
